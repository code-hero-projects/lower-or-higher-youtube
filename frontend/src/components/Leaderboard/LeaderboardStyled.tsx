import { Box } from '@material-ui/core';
import { styled } from '@material-ui/system';

export const LeaderboardWrapper = styled(Box)`
  display: flex;
  align-items: center;
  flex-direction: column;
`;

export const BackFromLeaderboardWrapper = styled(Box)`
  margin-top: 2rem;
`;