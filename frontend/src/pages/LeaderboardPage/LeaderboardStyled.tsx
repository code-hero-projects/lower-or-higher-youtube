import { Box } from '@material-ui/core';
import { styled } from '@material-ui/system';

export const LeaderboardWrapper = styled(Box)`
  height: 100vh;
  display: flex;
  flex-direction: column;
`;

export const LeaderboardElementWrapper = styled(Box)`
  margin-top: 4rem;
`;

export const LeaderboardTableWrapper = styled(Box)`
  margin: 2rem 10% 2rem 10%;
`;