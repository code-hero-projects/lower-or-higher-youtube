version: '3.4'

services:
  codehero.lowerorhigheryoutube.api:
    depends_on:
    - postgres
    image: ${DOCKER_REGISTRY-}codeherolowerorhigheryoutubeapi
    build:
      context: .
      dockerfile: CodeHero.LowerOrHigherYouTube.API/Dockerfile
    ports:
    - "5011:80"
  
  postgres:
    image: postgres:13.2
    restart: always
    environment:
      - POSTGRES_DB=HigherOrLowerYouTube
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=admin   
    ports:
    - "5432:5432"

  codehero.lowerorhigheryoutube.frontend:
    depends_on:
    - codehero.lowerorhigheryoutube.api
    image: ${DOCKER_REGISTRY-}codeherolowerorhigheryoutubefrontend
    restart: always
    ports:
    - "5010:80"